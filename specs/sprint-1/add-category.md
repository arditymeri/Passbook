# Spec: Category

**Sprint:** 1  
**Status:** Draft — awaiting review

---

## Goal

Allow a user to create and retrieve categories so that bills and incomes can be organised and later used for spending analysis.

---

## User Story

> As a user, I want to create categories and assign them a type (expense, income, or both), so that I can organise my transactions and understand my spending patterns.

---

## Acceptance Criteria

### Add category — happy path
- **Given** a valid request with a `name` and a `type`  
  **When** `POST /api/v1/categories` is called  
  **Then** the response is `201 Created` with the saved category including a generated `id`

### Add category — validation
- **Given** a request with no `name`  
  **When** `POST /api/v1/categories` is called  
  **Then** the response is `400 Bad Request`

- **Given** a request with a blank `name` (empty string or whitespace only)  
  **When** `POST /api/v1/categories` is called  
  **Then** the response is `400 Bad Request`

- **Given** a request with no `type`  
  **When** `POST /api/v1/categories` is called  
  **Then** the response is `400 Bad Request`

### Get category
- **Given** a category has been created  
  **When** `GET /api/v1/categories/{id}` is called with its id  
  **Then** the response is `200 OK` with the category

- **Given** no category exists with that id  
  **When** `GET /api/v1/categories/{id}` is called  
  **Then** the response is `404 Not Found`

### List categories
- **When** `GET /api/v1/categories` is called  
  **Then** the response is `200 OK` with all categories

- **When** `GET /api/v1/categories?type=EXPENSE` is called  
  **Then** the response is `200 OK` with only categories of type `EXPENSE`

---

## API Contract

### POST /api/v1/categories

**Request body** (`createCategoryRequest`):

| Field              | Type   | Required | Notes                        |
|--------------------|--------|----------|------------------------------|
| `name`             | string | ✅       | Must be non-blank            |
| `type`             | enum   | ✅       | EXPENSE, INCOME, BOTH        |
| `color`            | string | —        | e.g. hex code `#FF5733`      |
| `parentCategoryId` | string | —        | id of a parent category      |

**Responses:**
- `201 Created` — returns `categoryResponse`
- `400 Bad Request` — validation failed

### GET /api/v1/categories

**Query parameters:**

| Param  | Type | Required | Notes                          |
|--------|------|----------|--------------------------------|
| `type` | enum | —        | Filter by EXPENSE, INCOME, BOTH |

**Responses:**
- `200 OK` — returns `categoryListResponse`

### GET /api/v1/categories/{id}

**Responses:**
- `200 OK` — returns `categoryResponse`
- `404 Not Found`

---

## Domain Rules

1. `name` is required and must be non-blank
2. `type` is required — valid values: `EXPENSE`, `INCOME`, `BOTH`
3. `parentCategoryId` is optional — allows subcategories (e.g. "Restaurants" under "Food")
4. `color` is optional — used for UI display only, no format enforced at domain level
5. The `id` is generated by the database (UUID)

---

## Open Questions

- Should duplicate category names be rejected, or allowed?
- Should deleting a category that is referenced by a bill be blocked?

---

## Implementation Checklist

- [ ] OpenAPI spec (`category-model.yaml`, `category-post-controller.yaml`, `category-get-controller.yaml`)
- [ ] Domain unit tests (`AddCategoryServiceImplTest`)
- [ ] `AddCategoryService` / `GetCategoryService` updated with validation
- [ ] `AddCategoryPersistencePort` / `GetCategoryPersistencePort` (already exist — verify)
- [ ] `AddCategoryServiceImpl` updated with validation
- [ ] `CategoryEntity`, `CategoryRepository`, `CategoryMapper` (Infrastructure)
- [ ] `AddCategoryPostgresAdapter`, `GetCategoryPostgresAdapter`
- [ ] `CategoryMapper` (Application)
- [ ] `CategoryCreateController`, `CategoryGetController`
- [ ] Integration test (`CategoryControllerIntegrationTest`)

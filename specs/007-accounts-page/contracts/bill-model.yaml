# Contract snapshot: Application/src/main/resources/swagger/bill/bill-model.yaml
# This is the TARGET state for this feature. Only the `bill` schema's `accountId`
# property is new — everything else is unchanged from the current file.

components:
  schemas:
    billResponseModel:
      type: "object"
      properties:
        common:
          type: "object"
          properties:
            success:
              type: "boolean"
            message:
              type: "string"
        bill:
          $ref: '#/components/schemas/bill'
    billListResponseModel:
      type: "object"
      properties:
        common:
          type: "object"
          properties:
            success:
              type: "boolean"
            message:
              type: "string"
        bills:
          type: "array"
          items:
            $ref: '#/components/schemas/bill'
    bill:
      type: "object"
      properties:
        id:
          format: "uuid"
          type: "string"
        amount:
          type: "number"
          format: "double"
        description:
          type: "string"
        time:
          type: "string"
          format: "date-time"
        date:
          type: "string"
          format: "date"
        categoryId:
          type: "string"
        accountId:               # NEW — optional, links this bill to an Account.id.
          type: "string"         # Absent/blank means "no account association" (unchanged
                                  # default behaviour for every bill created before this feature).
        billItems:
          type: "array"
          items:
            $ref: '#/components/schemas/billItem'

    billItem:
      type: "object"
      properties:
        name:
          type: "string"
        amount:
          type: "number"
          format: "double"

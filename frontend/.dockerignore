# The build context here is frontend/, so this file governs it rather than the repository root's.
node_modules/
dist/
dist-ssr/
.env
.env.*
!.env.example
*.local

# Playwright output — never needed in an image
test-results/
playwright-report/
